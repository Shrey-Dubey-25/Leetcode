#include<iostream>
#include<vector>
#include<bits/stdc++.h>

using namespace std;

vector<int> majorityElement(vector<int>& v) {
    
    vector<int>ans{};
    int c=0,can=0;
    for(int i=0;i<v.size();i++){
        
    }
}

int main(){
    vector<int>v{1,2};
    majorityElement(v);
}